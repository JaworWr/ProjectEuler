# Fortran solutions
## Prerequisites
* gfortran
* [fotran-stdlib](https://stdlib.fortran-lang.org/index.html)
## Building
To build a solution `<problem-name>.f90` just run `make <problem-name>`. You can also run `make modclean` to clean the `.mod` files.
